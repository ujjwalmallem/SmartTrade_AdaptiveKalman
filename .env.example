# Copy to .env for local runs (never commit .env)
ALPACA_API_KEY=
ALPACA_API_SECRET_KEY=

# ML exit probability trigger (default 0.68). Grid-search 0.60–0.75 in backtests.
# CLI --ml-threshold overrides this when set.
ML_EXIT_THRESHOLD=0.68
