{
  "name": "smarttrade-paper-trading-scheduler",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker Cron Trigger that dispatches the paper-trading.yml GitHub Actions workflow.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.0"
  }
}
